# Creating a release

All releases are created via the `release` workflow.

1. In the Actions section of the repository, run the "Create a release" action
2. Set the target branch, stable releases should always use the "develop" branch
3. Click "Run workflow"

> [!Tip]
> To create a prerelease version, select "dev", "alpha", "beta" or "rc" as the release type.
